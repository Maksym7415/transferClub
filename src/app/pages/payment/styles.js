
import { makeStyles } from '@material-ui/core/styles';

export default () => makeStyles((theme) => ({
  formContainer: {
    // marginTop: '200px',
    position: 'static',
  },
  form: {
    // marginTop: '200px',
    paddingTop: '200px',
    display: 'flex',
    flexDirection: 'column',
    width: '400px',
  },
}));
